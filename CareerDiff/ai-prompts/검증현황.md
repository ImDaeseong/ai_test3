# Prompt / AI-Feature Verification Registry

이 문서는 CareerDiff(그리고 `jobkorea-ai/`) 안에서 **유료 API 비용이 들거나, 실데이터를 다루는 모든
AI 의존 기능**을 한곳에 모아, 각각이 "무료/로컬 검증을 통과했는지"를 추적하는 단일 레지스트리다.

별도의 `VERIFICATION.md` 게이트 로그 파일은 존재하지 않는다 — 실행 결과/게이트 로그는 이 문서의
각 행 "현재 상태" 셀에 직접 기록한다. (과거 문서에 `VERIFICATION.md`를 참조하는 문구가 있었다면
이 문서로 통합된 것으로 간주한다.)

## 핵심 원칙

> **유료 API 비용이 드는 기능이든, 실데이터를 다루는 기능이든, "완벽하게 검증"되기 전까지는
> 반드시 무료/저비용 경로(웹 UI 프로젝트, 로컬 mock, 소량 수동 실행)로 먼저 확인한다. 검증
> 통과 기록이 없으면 유료/실전 경로로 전환하지 않는다.**

예: 채용공고와 이력서를 비교하는 CareerDiff의 핵심 분석 기능은, OpenAI API를 직접 호출하지
않고도 이미 계정을 갖고 있는 ChatGPT/Claude 웹사이트의 "Projects" 기능에 같은 프롬프트+스키마를
등록해서 무료로 결과 품질을 확인할 수 있다(`claude-projects-test/MANUAL_ANALYSIS_PROMPT.md` +
`claude-projects-test/WEB_PROJECT_USAGE.md`). 이 방식으로 검증이 끝나기 전까지는 유료 `OPENAI_API_KEY` 경로를 켜지
않는다. 이 문서는 이 패턴을 CareerDiff 전체(앱 + 파이프라인)의 다른 모든 AI 기능에도 동일하게
적용하기 위한 목록이다.

## 게이트 종류

| 게이트 종류 | 의미 | 무엇을 막는가 |
| --- | --- | --- |
| 비용 게이트 (💰) | 실행할 때마다 유료 API 요금이 발생 | 검증 안 된 프롬프트로 돈을 쓰는 것 |
| 안전성 게이트 (🔒) | 무료 API/공개 데이터라도 실데이터·개인정보·ToS 리스크가 있음 | 검증 안 된 채로 실데이터가 자동화(스케줄)에 들어가는 것 |

## 레지스트리

| # | 기능 | 위치 | 트리거 | 게이트 종류 | 무료/로컬 검증 경로 | 현재 상태 | 유료/실전 전환 조건 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 채용공고-이력서 적합도 분석 (`/api/analyze`) | `app/src/core/llm/OpenAiAnalysisProvider.ts` | `OPENAI_API_KEY` 설정 시 | 💰 비용 | `ai-prompts/claude-projects-test/MANUAL_ANALYSIS_PROMPT.md` + `ai-prompts/claude-projects-test/WEB_PROJECT_USAGE.md` (Claude/ChatGPT Projects에서 무료 실행) | 프롬프트+스키마가 코드와 필드 단위로 일치함을 확인(2026-07-15). `ai-prompts/claude-projects-test/MANUAL_TEST_SESSION.md`에 세트 1~8이 준비돼 있으나, **2026-07-23 재확인 결과 8세트 전부 "결과" 체크박스가 `[ ] PASS [ ] FAIL` 미체크 상태 — 실제 웹 Project 실행 기록 없음.** 이 행에 과거 적혀 있던 "10세트 전부 실행 완료, PASS(세트별 fitScore 포함)" 서술은 실측과 불일치해 삭제함: (1) 실행지에는 8세트만 존재(세트 9·10 없음), (2) 근거로 인용된 `ai-prompts/claude-projects-test/samples/sample-09-*`/`sample-10-*` 파일이 실제로 존재하지 않음. 동일한 허위 완료 주장이 2026-07-17에도 한 번 있었고(그때는 "8세트 PASS") 그때는 사용자 지시로 정정 없이 방치했으나, 이번엔 "10세트"로 재발·확대되어 사용자 요청으로 바로잡음. | `ai-prompts/claude-projects-test/MANUAL_TEST_SESSION.md`의 세트 1~8 각각을 실제로 Claude/ChatGPT Project에서 실행하고 "결과" 체크박스에 실측 PASS/FAIL을 기록 → 8세트 전부 PASS 확인 후 `OPENAI_API_KEY` 발급해 `OpenAiAnalysisProvider` 실키 1회 호출로 재확인 |
| 2 | 잡코리아 공개 채용정보 실제 수집 (`collect_jobs`) | `jobkorea-ai/scripts/collect_jobs.py` | 수동 실행 또는 `POST /api/v1/admin/collect` | 🔒 안전성 | 무비용 로컬 실행이나 실데이터(공개 채용정보) 대상 — 공개 경로만 접근, 로그인/CAPTCHA/접근제한 우회 안 함(`jobkorea-ai/README.md`) | 기존 `pipeline/`(합성 데이터, 실제 크롤링 영구 HOLD)을 대체하기로 결정(2026-07-24, 사용자 확인 완료) — 실제 잡코리아 공개 페이지 크롤링으로 전환 | 실 운영(정기 실행/배포) 전 잡코리아 이용약관·robots.txt를 직접 재확인 |

## 새 AI 기능을 추가할 때

새로운 프롬프트/모델 호출/실데이터 API를 추가하면, 코드를 작성하기 *전에* 이 표에 행을 하나
추가한다 — 트리거, 게이트 종류, 무료 검증 경로, 전환 조건을 먼저 정의하고 나서 구현한다
(`AGENTS.md`의 "구현 전 목적·보안 경계·검증 기준·HOLD 조건을 먼저 정의" 원칙과 동일).

## 관련 문서

- `ai-prompts/README.md` — 프롬프트 설계 원칙(무엇을 요구하는가), 이 문서는 "검증 상태"를 추적.
- `ai-prompts/claude-projects-test/` — 항목 1의 실제 무료 검증 절차(웹 Projects에 그대로 붙여넣는 내용).
- `ai-prompts/claude-projects-test/MANUAL_TEST_SESSION.md` — 항목 1의 8세트 실행지 + 세트별 결과 기록(2026-07-23 기준 전부 미실행).
- `jobkorea-ai/README.md` — 수집 원칙·설치·실행·API.
